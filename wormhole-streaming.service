[Unit]
Description=Wormhole Streaming Server (TCP data plane + admin API)
After=network.target

[Service]
Type=simple
User=pi
WorkingDirectory=<INSTALL_DIR>
ExecStart=<INSTALL_DIR>/venv/bin/python streaming_server.py
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
